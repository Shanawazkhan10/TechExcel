import { combineReducers } from "redux";
import { FavList } from "./FavList";

// combine all reducers
export const rootReducer = combineReducers({
  FavList,
  // example (authReducers)
  // example (Add To Cart reducer)
});
